<?xml version="1.0" ?>
<article short="bluetooth" title="bluetooth - Bluetooth sockets">

<!-- ----+---------+---------+---------+---------+---------+---------+----- -->
<content>

<p>
The bluetooth module allows Bluetooth socket addresses, commonly used for 
connecting mobile devices and accessories, as well as computers.
</p>

<p>
<strong>NOTE:</strong> Only rfcomm (stream) socket types are supported at 
present.
</p>


<h2>SconeScript Interface</h2>

<p>
In addition to the standard module interface, it also has the following:
</p>

  <div class='scr-obj'>
  <h4>
  addr (
    <span>address : <em>string</em> , </span>
    <span>channel : <em>int</em> </span>
  )
  </h4>
  <p>
  Create a socket address for the specified Bluetooth address and channel.
  </p>

  <p>
  <em>address</em> can be: 
  <ul>
    <li>
    A hexadecimal Bluetooth device address in the form "aa:bb:cc:dd:ee:ff".
    </li>
    <li>
    An asterisk "*" - indicating a wildcard address. This is only valid when
    used with listening sockets and indicates that connections should be 
    accepted on all available local devices.
    </li>
  </ul>

  <p>
  <em>channel</em> should be a Bluetooth channel number.

  <p>
  If the address is required for listening, then <em>host</em> should specify
  a local device (or, more commonly, "*" for all) and <em>channel</em> 
  should specify a currently unused channel.
  </p>
  </div>

</content>
<!-- ----+---------+---------+---------+---------+---------+---------+----- -->

</article>
