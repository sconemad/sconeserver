<?xml version="1.0" ?>
<article short="local" title="local - Local (UNIX Domain) sockets">

<!-- ----+---------+---------+---------+---------+---------+---------+----- -->
<content>

<p>
The local module allows local socket addresses, which can be used for 
communicating between processes on the local machine.
</p>


<h2>SconeScript Interface</h2>

<p>
In addition to the standard module interface, it also has the following:
</p>

  <div class='scr-obj'>
  <h4>
  addr (
    <span>path : <em>string</em> </span>
  )
  </h4>
  <p>
  Create a socket address for the specified local path.
  </p>

  <p>
  <em>path</em> should be an absolute path to the socket on the local 
  machine.
  </p>

  <p>
  If the address is required for listening, then <em>path</em> should not
  already exist. If it does, then the file will be removed before listening
  takes place.
  </p>

  <p>
  Appropriate filesystem permissions are required for the path specified.
  </p>
  </div>

</content>
<!-- ----+---------+---------+---------+---------+---------+---------+----- -->

</article>
