<template>
<?scx

template("common");
template("account");
template("shorturl");
template("botdetect");
template("wiki");

if (defined(request.params["rss"])) {
  template("rss");

} else {
  template("normal");
}

?>
</template>
